# CTFd-solve-FX
Solve celebration (sound + fullscreen visual) plugin for your CTFD instance(s). Configurable from the admin panel, ships with 4 presets and 6 sound chimes, and exposes an extension API for building your own.
